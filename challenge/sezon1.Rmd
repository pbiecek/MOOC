---
title: "Challange"
author: "pogRomcy danych"
date: "sezon 0 / odcinek 0 <br><br> Wszelkie prawa zastrzeżone.<br> Wykorzystanie możliwe wyłącznie za zgodą autora.  <br><br><br><br>Naciśnij `A` by zamienić slajdy na ciągły tekst. <br> Naciśnij `T` by wyświetlić spis treści."
output:
  slidy_presentation:
    includes:
      in_header: ../tracking.html
    highlight: default
    css: ../style.css
    font_adjustment: 0
    footer: "pogRomcy danych / challange / sezon 1 "
---

# Poziom 1 - zadanie 1

Do zmiennej `wynik` przypisz wartość wyrażenia $sqrt((2+517)*1.23)$.

<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>

# Poziom 1 - zadanie 2

W zbiorze danych `koty_ptaki` w kolumnie `predkosc` znajduje się maksymalna prędkość rozwijana przez gatunek w kilometrach na godzinę.

Używajac funkcji `ifelse()` do zmiennej `wynik` przypisz wektor z wartościami `szybko` i `wolny` w zależności od tego czy zwierze jest szybsze niż 44 km/h czy nie.


<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>

# Poziom 1 - zadanie 3

Użyj funkcji `filter()` wybierz ze zbioru danych `koty_ptaki` tylko zwierzęta o długości ponad 1m. Przypisz tą nową ramkę danych do zmiennej `wynik`.

Nie zapomnij w tym rozwiązaniu najpierw włączyć pakiet `dplyr`,

<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>

# Poziom 1 - zadanie 4

Użyj funkcji `arrange()`, uporządkuj wiersze w zbiorze danych `koty_ptaki` rosnąco zgodnie z prędkością. 
Przypisz tą nową ramkę danych do zmiennej `wynik`.

Nie zapomnij w tym rozwiązaniu najpierw włączyć pakiet `dplyr`,

<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>

# Poziom 2 zadanie 5

W zbiorze danych `auta2012` w kolumnie `Marka` znajdują się nazwy marek oferowanych do sprzedaży samochodów.

Zbuduj wektor nazw marek samochodów i przypisz go do zmiennej `wynik`.

<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>

# Poziom 2 zadanie 6

W zbiorze danych `auta2012` w kolumnie `Wyposazenie.dodatkowe` po przecinku wypisane są elementy wyposażenia auta.

Jeżeli auto ma klimatyzację to w tej kolumnie znajduje się napis `klimatyzacja`. Sprawdź jak wiele aut ma klimatyzację.

Wyznacz macierz kontyngencji i zapisz ją do zmiennej `wynik`.

<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>


# Poziom 2 zadanie 7

Ze zbioru danych `auta2012` wybierz tylko auta marki `Volkswagen`.

Następnie średnią cenę wybranych samochodów przypisz do zmiennej `wynikCena` a średni przebieg przypisz do zmiennej `wynikPrzebieg`.

<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>


# Poziom 2 zadanie 8

Używając funkcji z pakietu `dplyr` wyznacz średnią cenę dla różnych marek.

Pogrupuj dane markami a następnie w tych grupach wyznacz średnią cenę.

<textarea rows="6" cols="70">
Tutaj wklej rozwiązanie. 
</textarea>

<button type="button">Wyślij rozwiązanie!</button>


# Poziom 3 zadanie 9

Ze zbioru danych `auta2012` wybierz tylko auta marki `Volkswagen`.

Wybierz kolumnę z rodzajem paliwa a następnie narysuj wykres słupkowy prezentujący jak często auta mają poszcezgólne rodzaje paliwa.


# Poziom 3 zadanie 10

Napisz funkcję, która przyjmuje jeden argument o nazwie `marka` a jako wynik zwraca procent aut tej marki, które są wyposażone w klimatyzację. Procent jest wyznaczany na bazie zbioru danych `auta2012`.

Zapisz tę funkcje pod nazwą `procKlima`.

# Poziom 3 zadanie 11

Ze zbioru danych `auta2012` wybierz samochody Toyota Corolla, a następnie narysuj historam cen tych ofert.


# Poziom 3 zadanie 12

Ze zbioru danych `auta2012` wybierz samochody Toyota, pogrupuj je po modelu, a następnie policz dla każdego modelu średnią cenę. 

Tak wyznaczoną ramkę z kolumna opisującą model oraz kolumną opisującą średnią cenę tego modelu posortuj po cenie, tak by najdroższe modele były na górze a najtańsze na dole.

Wynik przypisz do zmiennej `wynik`.

# Poziom 4 zadanie 13

Pod adresem `http://biecek.pl/R/dane/daneO.csv` znajduje się zbiór danych, w którego pierwszej kolumnie znajduje się zmienna `Wiek`. Policz średnią wartość z tej kolumny i przypisz do zmiennej `srWiek`.

Przykładowe rozwiązanie:

```{r}

dat <- read.table("http://biecek.pl/R/dane/daneO.csv", sep=";")
srWiek <- mean(dat$Wiek)

```

Test


```{r}

isCorrect <- ifelse(abs(srWiek - 45.5154) < 0.001, TRUE, FALSE)
comments <- ifelse(!exists("srWiek"), "Brak zmiennej o nazwie srWiek", 
                   ifelse(isCorrect, "", 
                          "To nie jest poprawna odpowiedź"))

```

# Poziom 4 zadanie 14

Dla zbioru danych `koty_ptaki` z pakiety `PogromcyDanych` policz maksymalną prędkość dla każdej grupy gatunków.

Wynik zapisz to zmiennej `maxPredkosc`.

Przykładowe rozwiązanie:

```{r, message=FALSE, warning=FALSE}
library(PogromcyDanych)
library(dplyr)

maxPredkosc <- koty_ptaki %>% 
  group_by(druzyna) %>% 
  summarise(max(predkosc)) %>% 
  `[`(,2) %>% 
  unlist()

```

Test

```{r}

isCorrect <- ifelse(all(maxPredkosc == c(115, 170)), 
                    TRUE, FALSE)
comments <- ifelse(!exists("maxPredkosc"), "Brak zmiennej o nazwie maxPredkosc", 
                   ifelse(isCorrect, "", 
                          "To nie jest poprawna odpowiedź"))

```


# Poziom 4 zadanie 15

POZOSTAŁE SĄ W BAZIE